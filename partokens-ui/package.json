{
  "name": "partokens-ui",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @partokens/web dev",
    "build": "bun --filter @partokens/web build",
    "typecheck": "bun --filter @partokens/web typecheck",
    "test": "bun test scripts/release-contract.test.ts scripts/deploy-config.test.ts && bun --filter @partokens/web test",
    "test:e2e": "bun --filter @partokens/web test:e2e",
    "compatibility:upstream": "bun scripts/check-new-api.ts",
    "compatibility:upstream:candidate": "bun scripts/check-new-api.ts --candidate",
    "check": "bun run typecheck && bun run test && bun run test:e2e && bun run build && bun run compatibility:upstream",
    "release:preflight": "bun scripts/release-check.ts environment",
    "release:staging:preflight": "bun scripts/staging-preflight.ts",
    "release:package": "bun scripts/package-release.ts",
    "release:verify": "bun scripts/release-check.ts package",
    "release:smoke": "bun scripts/smoke-release.ts",
    "release:staging:package": "PARTOKENS_RELEASE_CHANNEL=staging bun run release:package && PARTOKENS_RELEASE_CHANNEL=staging PARTOKENS_REQUIRE_CLEAN_SOURCE=true bun run release:verify",
    "release:staging:smoke": "PARTOKENS_SMOKE_ENVIRONMENT=staging bun run release:smoke",
    "release:staging": "bun run release:staging:preflight && bun run check && bun run release:staging:package",
    "release": "bun run release:preflight && bun run check && bun run release:package && bun run release:verify"
  }
}
